import React from 'react';
import PropTypes from 'prop-types';

const ComingSoon = (props) => (
    <React.Fragment>
        <span style={{paddingLeft: "10px",
    fontSize: "15px",
    color: "#f6a821"}}>Coming Soon</span>
    </React.Fragment>
);

export default ComingSoon;